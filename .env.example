# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file with real credentials!

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database_name

# Application Configuration  
NODE_ENV=development
PORT=8080

# API Configuration
VITE_API_URL=/api

# Security (generate a secure random string)
SESSION_SECRET=your-super-secure-session-secret-key

# Optional: Email Configuration
# SMTP_HOST=your-smtp-host
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# Note: Your actual environment variables are set securely using DevServerControl
# This file is just for reference and should not contain real credentials
